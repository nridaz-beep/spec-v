{
  "name": "spec-v",
  "version": "1.0.0",
  "dependencies": {
    "@supabase/supabase-js": "2.39.0",
    "stripe": "16.12.0"
  },
  "scripts": {
    "check": "node scripts/check-syntax.js && node scripts/check-release.js && node scripts/check-policy.cjs",
    "test": "npm run check && npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/unit/*.test.cjs",
    "test:e2e": "playwright test",
    "release:evidence": "node scripts/release-evidence.cjs create",
    "build": "node scripts/release-evidence.cjs verify && npm run check"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "chart.js": "4.5.1",
    "pdf-lib": "1.17.1"
  }
}
